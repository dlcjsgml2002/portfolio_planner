<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yi.mapper.MemberMapper">
	<insert id="insert">
		insert into member (id, pw, name, email, admin)
		values (#{id}, #{pw}, #{name}, #{email}, #{admin})
	</insert>

	<select id="read" resultType="Member">
		select *
		from member
		where id = #{id} and pw = #{pw}
	</select>
	
	<update id="update">
		update member
		set	pw = #{pw},
			name = #{name},
			email = #{email},
			admin = #{admin}
		where id = #{id}
	</update>
	
	<delete id="delete">
		delete
		from member
		where id = #{id}
	</delete>
	
	<select id="selectAll" resultType="Member">
		select *
		from member
	</select>
</mapper>